<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.trans.dao.StudentMapper">

    <sql id="Base_Column_List">
            id,
            username,
            "password",
            regtime
    </sql>
    <resultMap id="BaseResultMap" type="com.trans.entity.Student">
        <result column="id" property="id"/>
        <result column="username" property="username"/>
        <result column="password" property="password"/>
        <result column="regtime" property="regTime"/>
    </resultMap>
    <select id="selectAllStudent" resultType="com.trans.entity.Student">
        select * from student_transaction
    </select>

    <insert id="insertStudent">
        INSERT INTO student_transaction(
        username,
        password,
        regTime
        ) VALUES (
        #{username},#{password},
        #{regTime}
        )
    </insert>

<!--auto generated by MybatisCodeHelper on 2020-10-22-->
    <select id="selectAllByUsername" resultMap="BaseResultMap">
        select
        <include refid="Base_Column_List"/>
        from student_transaction
        where username=#{username}
    </select>

<!--auto generated by MybatisCodeHelper on 2020-10-22-->
    <update id="updateById">
        update student_transaction
        <set>
            <if test="updated.id != null">
                id = #{updated.id},
            </if>
            <if test="updated.username != null and updated.username != ''">
                username = #{updated.username},
            </if>
            <if test="updated.password != null and updated.password != ''">
                password = #{updated.password},
            </if>
            <if test="updated.regTime != null">
                regtime = #{updated.regTime},
            </if>
        </set>
        where id=#{id}
    </update>

<!--auto generated by MybatisCodeHelper on 2020-10-22-->
    <select id="selectById" resultMap="BaseResultMap">
        select
        <include refid="Base_Column_List"/>
        from student_transaction
        where id=#{id}
    </select>

</mapper>