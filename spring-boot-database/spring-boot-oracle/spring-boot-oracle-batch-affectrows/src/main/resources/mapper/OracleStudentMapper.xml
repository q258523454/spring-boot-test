<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.batch.dao.OracleStudentMapper">
    <resultMap id="BaseResultMap" type="com.batch.entity.Student">
        <id column="ID" jdbcType="INTEGER" property="id"/>
        <result column="NAME" jdbcType="VARCHAR" property="name"/>
        <result column="NAME2" jdbcType="VARCHAR" property="name2"/>
        <result column="AGE" jdbcType="DECIMAL" property="age"/>
    </resultMap>

    <sql id="Base_Column_List">
        ID,
        NAME,
        NAME2,
        AGE
    </sql>

    <select id="getMaxId" resultType="Long">
        SELECT MAX(id)
        FROM STUDENT
    </select>


    <select id="getMinId" resultType="Long">
        SELECT MIN(id)
        FROM STUDENT
    </select>

    <select id="selectByPrimaryKey" parameterType="java.math.BigDecimal" resultMap="BaseResultMap">
        SELECT
        <include refid="Base_Column_List"/>
        FROM
        STUDENT
        WHERE
        ID = #{id,jdbcType=NUMERIC}
    </select>

    <select id="selectAll" resultMap="BaseResultMap">
        SELECT
        <include refid="Base_Column_List"/>
        FROM
        STUDENT
        ORDER BY ID
    </select>


    <select id="selectAllByIdList" resultMap="BaseResultMap">
        SELECT
        <include refid="Base_Column_List"/>
        FROM
        STUDENT
        WHERE id IN
        <foreach collection="list" item="item" open="(" close=")" separator=",">
            #{item}
        </foreach>
    </select>

    <select id="selectByName" resultMap="BaseResultMap">
        SELECT
        <include refid="Base_Column_List"/>
        FROM STUDENT
        WHERE NAME = #{updated.name,jdbcType=VARCHAR}
    </select>

    <insert id="insert" parameterType="com.batch.entity.Student">
        INSERT INTO STUDENT(ID,
        NAME,
        AGE)
        VALUES (#{id,jdbcType=NUMERIC},
        #{name,jdbcType=VARCHAR},
        #{age,jdbcType=DECIMAL})
    </insert>

    <update id="update" parameterType="com.batch.entity.Student">
        UPDATE STUDENT
        SET NAME='update',
        AGE='18'
        WHERE ID = #{id}
    </update>

    <!--auto generated by MybatisCodeHelper on 2020-04-26-->
    <update id="updateByName">
        UPDATE STUDENT
        <set>
            <if test="updated.id != null">
                ID = #{updated.id,jdbcType=INTEGER},
            </if>
            <if test="updated.name2 != null">
                NAME2 = #{updated.name2,jdbcType=VARCHAR},
            </if>
            <if test="updated.age != null">
                AGE = #{updated.age,jdbcType=DECIMAL},
            </if>
        </set>
        WHERE NAME=#{name,jdbcType=VARCHAR}
    </update>

    <update id="updateById">
        UPDATE STUDENT
        <set>
            <if test="updated.id != null">
                ID = #{updated.id,jdbcType=INTEGER},
            </if>
            <if test="updated.name != null">
                NAME2 = #{updated.name,jdbcType=VARCHAR},
            </if>
            <if test="updated.name2 != null">
                NAME2 = #{updated.name2,jdbcType=VARCHAR},
            </if>
            <if test="updated.age != null">
                AGE = #{updated.age,jdbcType=DECIMAL},
            </if>
        </set>
        WHERE ID=#{id,jdbcType=INTEGER}
    </update>


    <insert id="insertListBatch">
        INSERT INTO STUDENT(
        ID,
        NAME,
        AGE
        )
        <foreach collection="studentList" item="item" index="index" separator="union all">
            (
            SELECT
            #{item.id},
            #{item.name,jdbcType=VARCHAR},
            #{item.age,jdbcType=DECIMAL}
            FROM dual
            )
        </foreach>
    </insert>

    <insert id="insertListBeginEnd">
        <foreach collection="studentList" item="item" index="index" open="begin" close=";end;" separator=";">
            INSERT INTO STUDENT(
            ID,
            NAME,
            AGE
            )VALUES(
            #{item.id},
            #{item.name},
            #{item.age}
            )
        </foreach>
    </insert>

    <update id="updateListByIdBatch">
        UPDATE STUDENT
        <trim prefix="set" suffixOverrides=",">
            <trim prefix="AGE =case" suffix="end,">
                <foreach collection="studentList" item="item" index="index">
                    <choose>
                        <when test="item.age != null">
                            WHEN ID=#{item.id} THEN #{item.age,jdbcType=DECIMAL}
                        </when>
                        <otherwise>
                            <!-- 字段为null, 用原值更新(保留原值), 否则会被重置为null -->
                            WHEN ID=#{item.id,jdbcType=DECIMAL} THEN age
                        </otherwise>
                    </choose>
                </foreach>
            </trim>
            <trim prefix="NAME =case" suffix="end">
                <foreach collection="studentList" item="item" index="index">
                    <choose>
                        <when test="item.name != null">
                            WHEN ID=#{item.id,jdbcType=DECIMAL} THEN #{item.name,jdbcType=VARCHAR}
                        </when>
                        <otherwise>
                            <!-- 字段为null, 用原值更新(保留原值), 否则会被重置为null -->
                            WHEN ID=#{item.id,jdbcType=DECIMAL} THEN name
                        </otherwise>
                    </choose>
                </foreach>
            </trim>
        </trim>
        WHERE ID IN
        <foreach collection="studentList" item="item" index="index" separator="," open="(" close=")">
            #{item.id,jdbcType=DECIMAL}
        </foreach>
    </update>

    <update id="updateListByIdAndIdBatch">
        UPDATE STUDENT
        <trim prefix="set" suffixOverrides=",">
            <trim prefix="AGE =case" suffix="end,">
                <foreach collection="studentList" item="item" index="index">
                    <choose>
                        <when test="item.age != null">
                            WHEN ID=#{item.id} THEN #{item.age,jdbcType=DECIMAL}
                        </when>
                        <otherwise>
                            <!-- 字段为null, 用原值更新(保留原值), 否则会被重置为null -->
                            WHEN ID=#{item.id,jdbcType=DECIMAL} THEN age
                        </otherwise>
                    </choose>
                </foreach>
            </trim>
            <trim prefix="NAME =case" suffix="end">
                <foreach collection="studentList" item="item" index="index">
                    <choose>
                        <when test="item.name != null">
                            WHEN ID=#{item.id,jdbcType=DECIMAL} THEN #{item.name,jdbcType=VARCHAR}
                        </when>
                        <otherwise>
                            <!-- 字段为null, 用原值更新(保留原值), 否则会被重置为null -->
                            WHEN ID=#{item.id,jdbcType=DECIMAL} THEN name
                        </otherwise>
                    </choose>
                </foreach>
            </trim>
        </trim>
        WHERE (ID,ID) IN
        <foreach collection="studentList" item="item" index="index" separator="," open="(" close=")">
            (#{item.id,jdbcType=DECIMAL},#{item.id,jdbcType=DECIMAL})
        </foreach>
    </update>


    <update id="updateListByIdBeginEnd">
        <foreach collection="studentList" item="item" index="index" open="begin" close=";end;" separator=";">
            UPDATE STUDENT
            <set>
                <if test="item.name != null">
                    NAME = #{item.name,jdbcType=VARCHAR},
                </if>
                <if test="item.name2 != null">
                    NAME2 = #{item.name2,jdbcType=VARCHAR},
                </if>
                <if test="item.age != null">
                    AGE = #{item.age,jdbcType=DECIMAL},
                </if>
            </set>
            WHERE ID=#{item.id,jdbcType=VARCHAR}
        </foreach>
    </update>


    <delete id="deleteByIdList">
        DELETE
        FROM
        STUDENT
        WHERE id IN
        <foreach collection="list" item="item" open="(" close=")" separator=",">
            #{item}
        </foreach>
    </delete>

    <delete id="deleteById" parameterType="java.lang.String">
        DELETE
        FROM
        STUDENT
        WHERE id=#{id}
    </delete>
</mapper>